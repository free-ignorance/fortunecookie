name: Default Node CI

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  lint:
    name: lints
    runs-on: ubuntu-latest
    steps:
      - name: Checking Out Commits Securely . . .
        uses: actions/checkout@v2
      - name: Setup Node 14 Environment . . .
        uses: actions/setup-node@v1
        with:
          node-version: 14
      - name: Install Dependencies . . .
        working-directory: .
        run: npm ci
      - name: Build Docker Image "free-ignorance/aphorismcookie"
        run: docker build -t free-ignorance/aphorismcookie .
      - name: Run The Lints . . .
        run: docker run free-ignorance/aphorismcookie npm run test:lint
  tests:
    name: tests
    runs-on: ubuntu-latest
    steps:
      - name: Checking Out Commits Securely . . .
        uses: actions/checkout@v2
      - name: Setup Node 14 Environment . . .
        uses: actions/setup-node@v1
        with:
          node-version: 14
      - name: Install Dependencies . . .
        working-directory: .
        run: npm ci
      - name: Build Docker Image "free-ignorance/aphorismcookie"
        run: docker build -t free-ignorance/aphorismcookie .
      - name: Run The Tests . . .
        run: docker run free-ignorance/aphorismcookie npm run test




